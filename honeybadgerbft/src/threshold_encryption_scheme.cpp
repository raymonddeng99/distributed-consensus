#include "threshold_encryption_scheme.h"

std::vector<Transaction> ThresholdEncryptionScheme::encrypt(const std::vector<Transaction>& transactions) {
    return transactions;
}

std::vector<Transaction> ThresholdEncryptionScheme::decrypt(const std::vector<Transaction>& encryptedTransactions) {
    return encryptedTransactions;
}